# MacsyZones 版本与发布规则

本文档是本项目长期有效的版本号和发布判定规则。它不依赖任何阶段性过程文档。

## 权威来源

- App 的当前构建版本唯一来源：`MacsyZones.xcodeproj/project.pbxproj` 中所有 Configuration 的 `MARKETING_VERSION`。
- 本项目已发布版本、上游基线和下一发布目标：[`RELEASES.md`](RELEASES.md)。
- Agent 的执行约束：[`AGENTS.md`](AGENTS.md)。
- 签名、构建和 DMG 操作：[`BUILD.md`](BUILD.md)。
- 阶段性设计和实施过程文档不属于版本号或发布规则的依据。

## 版本号的判定范围

版本级别按一次完整的 Goal 或一次完整的代码发布范围判定，不按单个 commit、单个文件或最后一个修复 commit 判定。

一次发布包含多个级别的变更时，必须采用最高级别：

```text
Major > Minor > Patch
```

不确定时采用较高版本级别，不得为了保持较小版本号而降低级别。

## 版本级别

### Major：`X.0.0`

满足以下任一条件，必须递增 Major：

- 持久化数据格式、配置语义或迁移规则发生不兼容变化。
- 修改显示器身份、Space、布局、窗口/AX/吸附等核心运行时边界，导致原有架构契约改变。
- 同一个 Goal 同时跨越以下核心域中的至少两个，并进行职责边界、数据流或状态模型重构：
  - 持久化与迁移；
  - 显示器与 Space；
  - 窗口、AX 与吸附；
  - 设置界面与交互状态；
  - 更新器、签名与发布。
- 一次架构重构修改了至少 10 个生产 Swift 文件，并且涉及至少两个核心域。
- 主要设置界面或核心交互流程被整体重做。
- 更新器、签名身份、Bundle ID、授权边界或发布安全边界发生重大变化。
- 虽然没有公开 API，但需要用户重新理解核心操作方式，或需要迁移/重新验证核心配置。

本项目是个人使用的 macOS App，不以“是否有第三方公开 API”作为 Major 的唯一标准；内部架构和配置契约同样属于版本兼容性。

### Minor：`X.Y.0`

只有在不满足 Major 条件时，满足以下任一条件才递增 Minor：

- 增加向后兼容的用户可见功能。
- 增加新的设置项、快捷键、布局操作或独立 UI 组件。
- 增加性能优化或兼容性支持，但不改变核心架构契约和数据迁移语义。
- 增加新的更新、诊断或可选行为，旧配置仍可直接使用。

### Patch：`X.Y.Z`

只有在不满足 Major 和 Minor 条件时，以下变更才允许递增 Patch：

- Bug 修复，且不改变配置格式、核心状态模型或用户操作流程。
- 文档、测试、日志、构建脚本或内部重构，且不改变运行时行为。
- 小范围 UI 文案、间距、颜色或可访问性标签调整。

“修改文件很少”不能单独证明是 Patch；“修改文件很多”也不能单独证明是 Major，必须结合核心域和架构契约判定。

## 发布前必须完成的版本决策

每个包含代码变更的 Goal 在开始实施前，必须记录以下内容；记录可以放在 Goal 计划、提交说明或对应 Release 记录中，但必须可追溯：

```text
基线版本：vX.Y.Z
变更核心域：持久化 / 显示器与 Space / 窗口与吸附 / 设置 UI / 更新器与发布
是否改变配置或迁移语义：是 / 否
是否改变核心架构边界：是 / 否
变更级别：Major / Minor / Patch
目标版本：vX.Y.Z
判定理由：一句话说明触发了哪条规则
```

本次回滚保留的是稳定代码，以及版本门禁、正式签名和可重复 DMG 构建改进；当前已发布版本为 `v1.2.5`，下一发布目标为 `v1.2.6 / Patch`。新架构和 UI 改造不进入后续版本。

## 自动阻断规则

发布前必须执行：

```bash
scripts/check-version.sh <target-version> <major|minor|patch>
```

该检查会阻断以下情况：

- `MARKETING_VERSION` 不唯一或与目标版本不一致。
- 目标版本不高于 `RELEASES.md` 中的当前已发布版本。
- 目标版本与声明的 Major/Minor/Patch 级别不匹配。
- `RELEASES.md` 中登记了下一发布目标，但命令使用了另一个版本或级别。

`scripts/build-release-dmg.sh` 会自动调用同一检查，因此不能只修改 DMG 文件名绕过版本校验。

## 两种不同的 Release 操作

### 新代码发布

只要代码、资源、配置迁移、构建脚本或签名流程发生变化，就必须：

- 重新判定版本级别；
- 使用新的、更高的版本号；
- 创建新的 Git tag 和 GitHub Release；
- 重新构建 DMG。

### 资产修复

只有在代码 commit、`MARKETING_VERSION`、Git tag 和 Release 正文完全不变，仅因上传失败或文件损坏而替换同一 DMG 资产时，才允许不递增版本号、不创建新 tag。资产修复不能包含代码变更。

## Tag 与上游版本

- 本项目只使用 `custom` 分支和本项目自己的 Release 记录。
- `upstream` 只用于拉取，不得推送。
- 仓库中继承的上游 `v3.x` tags 不代表本项目版本，不得用“最高 Git tag”推断本项目当前版本。
- 本项目版本必须以 `RELEASES.md` 和 `MARKETING_VERSION` 为准。
